export type MovieCardProps = {
	title: string;
	image: string;
	summary: string;
	rating: number;
};

export type Movie = {
	id: string;
	title: string;
	imageURL: string;
	summary: string;
	rating: number;
};
